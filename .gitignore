# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js / build output
/.next/
/out/
/build
/dist

# Supabase CLI local temp (created by supabase CLI probes; not part of the repo)
/supabase/.temp/
next-env.d.ts
*.tsbuildinfo

# Environment / secrets — NEVER commit
.env
.env.*
!.env.example
.env.local
.env*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.pem

# Vercel
.vercel

# Claude Code local session/config
.claude/

# packed skill archives — the unpacked .claude/skills/<name>/ dir is the canonical form
*.skill

# One-off admin provisioning script — local only, never committed
/scripts/provision-admin.ts
