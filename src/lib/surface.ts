// Visual-only: the elevated-surface recipe, shared so every panel matches.
//
// Solid warm-white card (bg-card token) + warm hairline border + restrained,
// warm-tinted shadow + rounded geometry. No backdrop-blur — the brand direction
// is "elevated surfaces," not frosted glass. Apply to panel containers only.
export const surfacePanel =
  "rounded-2xl border border-border bg-card shadow-[0_1px_2px_rgba(22,29,23,0.05),0_14px_34px_-18px_rgba(22,29,23,0.22)]";
